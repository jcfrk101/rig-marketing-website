# Base URL of rig-web-services. The fleet service-metrics endpoint is public
# (gated by a fixed admin id), matching how fleet.bigrig.app pulls live stats.
NEXT_PUBLIC_API_URL="https://api.bigrig.app"
